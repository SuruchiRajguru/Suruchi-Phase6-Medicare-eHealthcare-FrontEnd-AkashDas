import { Productrequestmodel } from './productrequestmodel';

describe('Productrequestmodel', () => {
  it('should create an instance', () => {
    expect(new Productrequestmodel()).toBeTruthy();
  });
});
