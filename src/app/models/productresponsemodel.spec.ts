import { Productresponsemodel } from './productresponsemodel';

describe('Productresponsemodel', () => {
  it('should create an instance', () => {
    expect(new Productresponsemodel()).toBeTruthy();
  });
});
